"""
watcher/views.py
----------------
Template-based views have been removed.
All watcher functionality is handled through the JSON API at /api/watcher/*.

The React frontend is the only client — no server-side rendered pages needed.

API routes (see wastewatch/api_urls.py):
  GET/POST /api/watcher/reports/
  GET/PUT  /api/watcher/reports/<id>/
  GET      /api/watcher/stats/
  POST     /api/watcher/confirm/
"""
